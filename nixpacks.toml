[phases.setup]
nixPkgs = ['nodejs_20', 'npm-10_x']

[phases.install]
cmds = ['npm ci --prefer-offline --no-audit']

[phases.build]
cmds = ['npm run build']

[phases.build.cacheDirectories]
paths = ['node_modules/.cache', 'dist']
